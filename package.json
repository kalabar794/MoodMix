{
  "name": "moodmix-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd moodmix && npm ci && npm run build",
    "dev": "cd moodmix && npm run dev",
    "start": "cd moodmix && npm start"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}